#cloud-config
package_update: true
packages:
  - docker.io
  - docker-compose-v2
  - awscli
  - jq

runcmd:
  - systemctl enable docker
  - systemctl start docker
  - mkdir -p /opt/openclaw
  - echo "cloud-init baseline ready" > /opt/openclaw/STATUS
